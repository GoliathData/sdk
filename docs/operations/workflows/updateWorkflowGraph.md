<!-- GENERATED FILE — do not edit. Regenerate with `yarn generate` in sdk/ (manifest snapshot → docs; see scripts/generate-docs.mjs). -->

# updateWorkflowGraph

mutation · domain `workflows` · requires the WRITE scope

Replace a workflow version's node/edge graph AND its triggers. Edit the DRAFT — get one from createOrReturnWorkflowDraft. A PAUSED version is also writable (UpdateWorkflowService accepts DRAFT and PAUSED), but editing one is off the normal path and it changes what the response tells you: see the draftValidation note below. workflowGraph is the superjson-serialized WorkflowGraph shape returned by getWorkflowVersionGraph; read the existing graph first and send back a modified copy, mirroring the exact serialized field names it returns (node/edge ids are caller-minted strings). A valid graph needs at least a trigger node and an end node connected by an edge; every node is {id, type, config} and every edge is {id, config, parentNodeId, childNodeId} (edge config is {} except conditional branches, below). NODE VOCABULARY — the full set of authorable step types; workflows are NOT limited to text steps. Structural (all domains): "trigger" (config {}), "end" (config {}), "wait" (config {mode:"fixed", seconds} or {mode:"range", minSeconds, maxSeconds}; REQUIRES an interruptFilter FilterGroup — pair an empty group {id,operator:"AND",conditions:[],groups:[]} with interruptEnabled:false for no early exit, or a real filter with interruptEnabled:true to end the wait early, e.g. on an inbound reply), and "conditional" (every domain except EVENT; config {filter: <FilterGroup>}; give it TWO outgoing edges — edge config {eval:true} is followed when the record matches the filter, {eval:false} when it does not). THAT FilterGroup IS THE FULL SAVED-FILTER VOCABULARY FOR THE DOMAIN, not a reduced automation-only subset — so anything you can filter on in that domain, a workflow can branch on. For CONTACT and PROPERTY that vocabulary is enumerable: listFilterFieldNames returns it complete, and a field it lists is a field a conditional can evaluate. NEVER tell a user an automation cannot consider some attribute without reading that list for the workflow DOMAIN first — and read the right domain, because the vocabularies differ: property attributes (last sold date, year built, estimated value) are evaluable in a PROPERTY workflow and are not contact fields, so "the automation cannot see it" is usually a domain mismatch rather than a missing capability. DEAL and APPOINTMENT conditionals take their own full filter group in exactly the same way, but there is NO catalog operation for those two — listFilterFieldNames and listFilterFields accept CONTACT and PROPERTY only and reject anything else as invalid input. That is a gap in DISCOVERY, never evidence about the capability: read the existing graph with getWorkflowVersionGraph and copy the shape of a conditional already in it, and if you cannot establish that a field is evaluable there, say you could not confirm it rather than reporting that it cannot be done. GOLIATH-ADMIN ONLY, every domain except EVENT: "ai_conditional" — the same two-edge {eval:true}/{eval:false} fork as "conditional", decided by a MODEL reading the record instead of by a filter. config {prompt, model, context}. `prompt` is a yes/no question about the record (true takes the {eval:true} edge). `model` is one of gemini-2.5-flash-lite | gemini-2.5-flash | gemini-2.5-pro | gpt-5-mini | gpt-5 | claude-haiku-4-5 | claude-sonnet-5 | grok-4-1-fast-non-reasoning. `context` says what the model is allowed to READ, as a Context Layer selection: an object whose keys are any of contact | deal | property | appointment | organization, each mapping to that source's slice flags (e.g. {contact:{includeTextMessages:true, includeNotes:false}}). A key's PRESENCE is the include decision; an absent key is not sent at all, and an empty object {} means that source with its provider defaults. Only the sources the RUN carries an id for resolve — a CONTACT run has a contact, a PROPERTY run a property — so a selection naming a source the domain's runs never carry renders nothing. Every evaluation is a billed model call on the org, so do not author one where a "conditional" filter would answer the same question. Note this node IS available on APPOINTMENT graphs even though "conditional" is not. CONTACT actions: "text_action" (SMS to the contact; allowed only in workflowType TEXT; config {mode:"single", textBody or templateId} | {mode:"multiple", textBodies:[...]} (rotates one per send) | {mode:"generative", systemPrompt}); "email_action" (only in workflowType EMAIL; config {emailBody, emailSubject?} | {templateId} | {generate:true, systemPrompt}); "notification_action" (text a TEAMMATE, not the contact; config {recipientType:"POINT_PERSON"} | {recipientType:"SPECIFIC_USER", recipientUserIds:[...]} | {recipientType:"PHONE_NUMBER", recipientPhoneNumber}, always with textBody. SPECIFIC_USER takes a LIST and texts every id on it — one message each, and a named user who has left the org or has no number is skipped rather than failing the step. The singular `recipientUserId` is the pre-list spelling: still accepted and still read, but send `recipientUserIds`, and never send both); "tags_action" (apply tags; config {tagIds:[...]}); "add_user_group_action" (add the contact to team(s), optionally auto-assigning an agent from a team; config {userGroupIds:[...], agentAssignment?: {kind:"NONE"} | {kind:"TEAM_ROUND_ROBIN", userGroupId} | {kind:"SPECIFIC_USER", userId}}); "contributors_action" (attach users to the contact; config {contributors:[{userId, status:"POINT_PERSON"|"PARTICIPANT"}], roundRobin?:{userIds:[...], role} — its presence switches to round-robin: one user per run, rotating. TWO REQUIRED BOOLEANS, both rejected if omitted rather than defaulted: sendNotificationToContributor notifies the user who was added/assigned, and replaceExistingAgent decides whether an Agent this step picks may DISPLACE the contact's current Agent — with it false, a contact that already has an Agent keeps it and the pick is added as a Participant instead. They are required because a silent default is what an omission used to get, and it was the WRONG one: the editor seeds both false on a new node while an omitted field meant the old notify-and-replace behaviour, so the same node authored two ways behaved differently. Send both; false, false is what the builder gives a new node); "set_custom_field_action" (config {customFieldId, customFieldName, customFieldType:"TEXT"|"NUMBER"|"DOLLAR"|"DATE"|"DROPDOWN"|"LINK"} plus the matching value field textValue | numberValue | dateIso | dropdownOptionId); "task_action" (create a task; config {name, taskType, dueOffset:{amount, unit:"DAYS"|"WEEKS"|"MONTHS"}, preferredTimeOfDay:{hour, minute, timezone}, assigneeUserIds:[...], usePointPersonAssignee?, description?, createGoogleCalendarEvent?}); "appointment_action" (like task_action with {startOffset, preferredStartTimeOfDay, durationMinutes, attendeeUserIds}); "deal_action" (create a deal; config {title, pipelineId, stageId, propertyConfig:"none"|"add_all_verified_properties"|"add_all_properties"|"add_first_verified_property", assigneeUserIds?, usePointPersonAssignee?, description?}); "ai_employee_action" (put an AI employee on the contact; config {agentId, runNow:boolean, schedule?:{cronExpression, timezone, maxRuns}}); "send_webhook_action" (config {urls:[...], auth?}, where `auth` is how the request authenticates and CARRIES THE SECRET ITSELF: {kind:"HEADER", headerName, secret} sends the secret under that header name, {kind:"HMAC", secret} sends no secret and signs the exact request body into X-Goliath-Signature. Omit `auth` for an unauthenticated POST. headerName must be an RFC 9110 token (no spaces, colons or newlines), may not be padded with whitespace, and may not be one the sender sets itself (content-type, x-goliath-idempotency-key, x-goliath-signature); `secret` must be non-empty. A step that violates any of those is saved but STOPS at run time rather than sending unauthenticated. NOTE the secret is stored in the node config in plain text and is therefore readable in this graph and in the run log); "send_slack_action" (config {slackChannelId, slackChannelName, message:{text, blocks?}}); "trigger_contact_workflow_action" (start other contact workflows on this contact; config {workflowGroupIds:[...]}); "enrich_action" (skiptrace the contact and fold the result back onto it — the same enrichment the Enrich button and bulk enrich run; config is {} and MUST be empty, extra keys are rejected rather than ignored: there is no address, targeting or retry option to set here. Already-enriched contacts dedupe server-side, so a workflow that reaches this step twice neither re-skiptraces nor charges twice; it spends SKIPTRACE credits per skiptrace that actually runs, and an org out of balance leaves the request unfulfilled rather than failing the run). DEAL actions: "assign_to_user_action" ({userIds:[...]}), "notification_action" ({recipientType:"DEAL_USERS"} | {recipientType:"SPECIFIC_USERS", userIds}, always with textBody), "create_deal_action" (same config as CONTACT deal_action), "send_slack_action", "trigger_contact_workflow_action". PROPERTY actions: "create_contact_action" ({contactListIds?, triggerContactWorkflowGroupIds?, contributorUserIds?, assignAgent?:{mode:"single_user", userId} | {mode:"round_robin", userIds}, deal?, and ONE REQUIRED BOOLEAN, rejected if omitted rather than defaulted: replaceExistingAgent, which decides whether an Agent this step picks may DISPLACE the contact's current one — this is a find-or-create, so it lands on contacts that already have an owner, and displacing DELETES the previous owner's mapping rather than demoting them to Participant. It is required because an omission used to mean `true`, and the editor seeds `false` on a new node, so the same node authored two ways behaved differently. Send `false` unless you mean to displace), "add_to_list_action" ({listIds:[...]}), "send_slack_action", "send_webhook_action". APPOINTMENT actions: "text_action" (single mode only), "trigger_contact_workflow_action", "send_slack_action". EVENT actions (config values are accessor paths into the webhook payload): "find_create_contact_action" ({mode:"field_mapping", nameAccessor, phoneAccessors?, emailAccessors?, propertyAddressAccessors?, mailingAddressAccessors?, noteAccessors?, customFieldMappings?} | {mode:"transformer", hint?}). customFieldMappings is field_mapping ONLY and is a list of {customFieldId | attributionKey, accessor | literalValue, valueMappings?:[{from,to}], unmappedValuePolicy?:"pass_through"|"drop"}: name an ordinary contact custom field by ID, or one of the org's reserved attribution fields by KEY (utm_source, utm_medium, utm_campaign, utm_term, utm_content, gclid, fbclid, msclkid, ttclid, li_fat_id, landingUrl, referrerUrl) — those are hidden from every custom-field listing, so the key is the only way to address one. Give each mapping EXACTLY ONE destination (customFieldId OR attributionKey) and EXACTLY ONE value source (accessor, a payload path, OR literalValue, a constant). A mapping naming two of either is REJECTED at execution — dropped and counted, never resolved by precedence — because guessing which half you meant would silently write a field you did not name. valueMappings translates the payload's vocabulary into the org's, matched case-insensitively; a value no rule names follows unmappedValuePolicy (default pass_through). Mapping the Source field replaces this step's default source of "Webhook" rather than being overwritten by it. tagMappings is field_mapping ONLY and is the same shape without a destination field: a list of {kind:"tag"|"list", accessor | literalValue, valueMappings?, unmappedValuePolicy?}. It resolves each value to a free-form TAG or a contact LIST by NAME — never by id, because a payload can only carry a name — and CREATES the tag or list when the org has none, so a workflow can tag every lead with the campaign it came from without the org pre-declaring every campaign. One row may resolve many values (an accessor landing on an array attaches each), deduplicated case-insensitively and bounded at 25 distinct values per row. Values that are obviously not category names — a uuid, an email, a url, a long-digit platform id, over 80 characters — are dropped rather than minted, the same plausibility gate the dropdown automation door uses. Tag and list names share ONE org-wide namespace across every ContactTag type, so a name a list already holds cannot also become a tag: that row is dropped and counted, never resolved onto the wrong row. Like customFieldMappings, a row naming BOTH accessor and literalValue is rejected at execution. Both counts ride the run's FIND_CREATE_CONTACT_RESOLVED audit event as tagsWritten/tagsDropped, "event_find_property_action" (flat config; resolutionMode:"full_address"|"address_components"|"parcel_id"|"transformer", actionType:"add_to_list"|"skiptrace"|"skiptrace_create_contact", plus the accessor/id fields each mode needs), "event_send_slack_action" ({slackChannelId?, slackChannelName?, message} — plain text with {{accessor}} placeholders). SLACK actions (the run subject is the message that started it; templates read it with the SLACK merge fields below, e.g. {{slack.text}}): "slack_reply_action" ({target:"TRIGGERING_THREAD"|"CHANNEL", message, and slackChannelId+slackChannelName only when target is "CHANNEL"} — in thread mode the destination comes from the triggering message, so a channel named there would be collected and never read).  A node type you see in a stored graph but not in this list is RETIRED (parse-compat only): never author one and never copy one into a new graph. MERGE FIELDS — the COMPLETE per-domain vocabulary a body may draw on. Substituted at run time: textBody, textBodies, emailBody, emailSubject, a Slack block's text and a Slack button's url, plus a deal node's title and a task node's name and description. NOT substituted, so braces there are left alone: a generative node's systemPrompt (an instruction to a model, not a template) and a deal node's description. Keys are matched EXACTLY and are case-sensitive. There is NO dotted record namespace: {{contact.address1}}, {{contact.firstName}} and {{deal.title}} are not merge fields (the SLACK keys below are the only dotted ones, and they are literal names, not a namespace you may extend). Do not infer a key from another domain's spelling either — the shapes genuinely differ (CONTACT has contactPropertyAddress, PROPERTY has full_address), so use a key off this list or none. An unknown token does NOT render blank and is NOT reported as a missing variable: renderTemplate returns the braces verbatim, so the recipient is texted the literal braces. promoteWorkflowDraft REFUSES a draft carrying one in a SCANNED FIELD — textBody, textBodies, emailBody (including one stored as emailBodyFormat "TIPTAP_JSON", whose merge fields are document pills rather than braces and are read as such), a NODE'S OWN emailSubject, a Slack block's text or its button url, a deal node's title and fallbackDealTitle on a CONTACT or DEAL graph, and an inline task node's name and description — and updateWorkflowGraph returns that same judgement as workflowGroup.draftValidation on every write — a reading of the GROUP'S DRAFT, which is the version you just wrote whenever you followed the normal path and edited the draft. Edit a PAUSED version instead and the field still answers about the draft, so it is null when there is none and about a DIFFERENT version when there is: it is not a verdict on your write in that case, and nothing in the response says so ({isValid, errors}, each unrenderable token named with the field it sits in and the supported key it most likely meant). READ IT on the response to the write and fix what it names, rather than discovering it when activation fails. THAT LIST OF SCANNED FIELDS IS CLOSED, and it is still SHORTER than the list of fields that are substituted. Anything substituted at run time but absent from it is checked by nothing, so a clean draftValidation is not evidence about it — check those against the vocabulary below yourself. What remains unscanned, and NOT an exhaustive list: the WORKFLOW-LEVEL email subject on communicationConfig — which is the subject that actually ships whenever sendInSameThread is not false, i.e. by default, and the scan only ever walks NODES; every FALLBACK field (fallbackTextBody, fallbackEmailSubject, fallbackEmailBody), each substituted by its executor under SEND_FALLBACK; a PROPERTY create-contact step's deal title and fallback title (substituted against the NEW CONTACT and the workflow user, not against the PROPERTY graph's own vocabulary, so the gate has no single vocabulary to judge them by and deliberately does not try); a Slack reply node's message; and any body that lives in a referenced templateId rather than in the graph — the graph then carries no body for the scan to look at. In every one of them a wrong key promotes clean. What the recipient then sees differs by path and the difference matters: most render through the token renderer and arrive as LITERAL BRACES, but a Slack reply node's message is interpolated by its own function, which replaces any {{slack.<field>}} it does not recognise with an EMPTY STRING — so a typo there is not visible as a broken template at all, it is simply missing text. PROPERTY: {{line1}}, {{line2}}, {{propertyCity}}, {{state}}, {{zip}}, {{full_address}}, {{property_url}}, {{beds}}, {{baths}}, {{sqft}}. CONTACT: {{fullName}}, {{firstName}}, {{lastName}}, {{contactUrl}}, {{contactPhoneNumber}}, {{contactEmail}}, {{contactPropertyAddress}}, {{contactPropertyFullAddress}}, {{contactPropertyCity}}, {{contactPropertyZipcode}}, {{userFullName}}, {{userFirstName}}, {{userLastName}}, {{userPhoneNumber}}, {{userEmail}}, {{userOrganizationName}}. DEAL: {{dealTitle}}, {{dealUrl}}, {{dealPipelineName}}, {{dealStageName}}, {{dealPropertyLine1}}, {{dealPropertyLine2}}, {{dealPropertyCity}}, {{dealPropertyState}}, {{dealPropertyZip}}, {{dealPropertyFullAddress}}, {{dealContactFullName}}, {{dealContactFirstName}}, {{dealContactLastName}}. APPOINTMENT: {{appointmentStartTime}}, {{appointmentEndTime}}, {{appointmentMeetingLink}}, {{appointmentLocation}}, {{appointmentDurationMinutes}}, {{fullName}}, {{firstName}}, {{lastName}}, {{contactUrl}}, {{contactPhoneNumber}}, {{contactEmail}}, {{contactPropertyAddress}}, {{contactPropertyFullAddress}}, {{contactPropertyCity}}, {{contactPropertyZipcode}}, {{userFullName}}, {{userFirstName}}, {{userLastName}}, {{userPhoneNumber}}, {{userEmail}}, {{userOrganizationName}}. EVENT: no merge fields at all — a {{...}} in an EVENT body is an ACCESSOR PATH into the inbound webhook payload (see the EVENT actions above), not a merge field, and is never validated against this vocabulary. SLACK: {{slack.text}}, {{slack.userName}}, {{slack.channelName}}, {{slack.permalink}}.  Two conditional keys, and neither is legal on every graph: {{agentName}} resolves ONLY where the graph's TEXT sender is a marketing campaign, and a contact CUSTOM FIELD — addressable as {{ContactTag_<uuid>}}, where the uuid is a CUSTOM-FIELD id from listContactCustomFields and NOT a tag id from searchContactTags, however much the prefix suggests otherwise (ContactTag is the table that stores tags, lists and custom fields alike, so the GRN is named for the table rather than for the thing; a real tag id passes the shape check, is accepted by promote, and renders empty) — resolves ONLY on CONTACT and APPOINTMENT graphs, the ones a send loads contact context for — and on an APPOINTMENT graph only from the TEXT REMINDER, which renders with the task, the contact and the user. An appointment Slack post loads the task alone, so every CONTACT key there, tags included, renders literally even though promote accepts it: the gate judges per DOMAIN, and this is the one place a domain is wider than one of its own nodes. Used outside its condition either one is unrenderable and blocks promote exactly like an unknown key.Triggers are managed through the `triggers` array: each entry is upserted by its id (mint a fresh UUID to add one) and entries omitted from the array are detached; only `config` syncs (`name` is cosmetic). Trigger config shapes: {type: "manual"}, {type: "created"}, {type: "mutation", filter: <FilterGroup>}, {type: "match", filter: <FilterGroup>}; CONTACT-only: {type: "inbound_text", twilioPhoneNumberIds: [...]}, {type: "inbound_email", connectedAccountIds?: [...]}; EVENT workflows have exactly one trigger shape, {type: "event_received"} — the workflow's minted webhook URL is the trigger. Minimal valid drip example (node/edge shape): nodes: [{id:"n1",type:"trigger",config:{}}, {id:"n2",type:"text_action",config:{mode:"single",textBody:"Hi {{firstName}}"}}, {id:"n3",type:"wait",config:{mode:"fixed",seconds:172800,interruptEnabled:false,interruptFilter:{id:"f1",operator:"AND",conditions:[],groups:[]}}}, {id:"n4",type:"end",config:{}}], edges: [{id:"e1",config:{},parentNodeId:"n1",childNodeId:"n2"}, {id:"e2",config:{},parentNodeId:"n2",childNodeId:"n3"}, {id:"e3",config:{},parentNodeId:"n3",childNodeId:"n4"}]. PROMOTE GOTCHA: a template-less TEXT/EMAIL draft starts with communicationConfig.sender {type: "UNSET"}, and promoteWorkflowDraft rejects SenderNotChosen — set a real sender in the automation config (e.g. {type: "POINT_PERSON"}) before promoting; getWorkflow.draftValidation names this and any other blocking issues. GRAPH CONFIG — `graph.config`, the settings an operator asks for by name. It sits beside `nodes`/`edges`/`triggers` and is NOT per-node. Shared keys (every domain): `triggerOperator` "AND"|"OR" (whether a graph with several triggers needs all of them or any one); `activation.triggerEligibleRecordsNow` (on activation, enroll records that ALREADY match, not just future ones); and `triggerWindow`, which limits when ENROLLMENT may happen; `exclusiveEnrollment` true means a record entering THIS workflow is unenrolled from every OTHER workflow it is still running under — those runs are STOPPED at the moment this one is created (other WORKFLOW, so a prior version of this same workflow is left alone), which is how "only one campaign at a time per lead" is expressed. It is inert on EVENT graphs, whose runs anchor on a webhook payload rather than a record. CONTACT graphs add `exitConditions.exitConditionTypes` — any of "any_inbound_communication", "inbound_text", "inbound_email", "contact_archived" — which is how "stop the drip as soon as they reply" is expressed; there is no per-node way to say it. APPOINTMENT graphs accept the `exitConditions` key but it carries nothing (typed `undefined`), so exit conditions are effectively CONTACT-only. `communicationConfig` is NOT contact-only: CONTACT and APPOINTMENT graphs both take it, and it is where an appointment reminder's quiet hours and sender live too. SEND WINDOW — `sendWindow` (on communicationConfig) and `triggerWindow` (top level) are the same shape: {timezone, daysOfWeek?:["MON".."SUN"], startTime?:{hour,minute}, endTime?:{hour,minute}, timezoneMode?:"RECIPIENT"|"CONFIGURED"}. `hour` AND `minute` are BOTH REQUIRED on each time — {hour:9} alone is rejected by the graph parser. These are clock times, not offsets, so "weekdays 9-6 Central" is daysOfWeek MON..FRI with startTime {hour:9,minute:0} and endTime {hour:18,minute:0}. A channel that configures NOTHING is not unrestricted: it falls back to 08:00-21:00 Eastern, the TCPA envelope. Say that rather than implying a missing window means any hour. `timezoneMode` says WHOSE clock those times are read on, and it changes when a message actually goes out: "RECIPIENT" (the default, and what an omitted field means) reads them in each recipient's own zone, derived from their phone number's area code — 9:00 means 9:00 where the lead is, so one schedule fires at a different instant per contact. "CONFIGURED" reads them in the window's own `timezone` for everybody, which is what an operator means by "send on MY schedule"; it is honoured only where a recipient zone exists to differ from (TEXT), and it never sends outside 08:00-21:00 in the recipient's local time — quiet hours still floor it. EMAIL has no area code to derive a zone from and always reads the configured `timezone`, so `timezoneMode` is inert there; so is a `triggerWindow`, which gates ENROLLMENT and has no recipient. SENDER — `communicationConfig` selects who a message comes FROM, and a mode name alone is not a valid sender: most modes carry required fields. TEXT: {type:"UNSET"} | {type:"POINT_PERSON"} | {type:"MEETING_OWNER"} (appointment graphs) | {type:"PHONE", fromPhoneNumberId} | {type:"RANDOM_PHONE", fromPhoneNumberIds:[...]} | {type:"LOCAL_PRESENCE", scope, fallback} — LOCAL_PRESENCE picks a team number whose area code matches the recipient, and BOTH scope and fallback are required: scope is {kind:"ALL_TEAM"} | {kind:"SPECIFIC", fromPhoneNumberIds} | {kind:"EXCLUDE", excludedPhoneNumberIds} | {kind:"MARKETING_CAMPAIGN", ...}, and a MARKETING_CAMPAIGN scope accepts ONLY the {type:"SCOPE_POOL"} fallback. EMAIL: {type:"UNSET"} | {type:"POINT_PERSON"} | {type:"IDENTITY", fromEmail, fromClientId} | {type:"RANDOM_IDENTITY", identities:[...]}. UNSET is the creation default and activation is GATED on replacing it, so a graph left on UNSET will not run. SYSTEM CATEGORY — the separate `systemCategory` variable, not part of the graph. CONTACT, DEAL, EVENT and APPOINTMENT all allow CUSTOM workflows, so `null` is correct there — do NOT attach a category to make an appointment reminder. PROPERTY is the one domain that allows no custom workflow at all: it MUST carry "PROPERTY_SIGNAL_SKIP_TRACE", and creating one without a category is rejected with "Custom workflows are not available for property — pick a template." A non-null `systemCategory` ALSO requires a `templateId` on the same create — "A system-category workflow needs a template id" — so a category alone still fails. Both rejections read as "property workflows are not supported" if you do not know the pair is required. One category exists that NO domain offers at create time: "APPOINTMENT_REMINDER" is a stored value you may READ on an existing workflow, but nothing lists it as creatable, so never pass it on a create — the appointment domain takes custom workflows instead.

## Call

```ts
const result = await client.workflows.updateWorkflowGraph({ workflowAutomationId: '<id>', workflowGraph: <SuperJSON> }, { idempotencyKey: crypto.randomUUID() })
// → Promise<UpdateWorkflowGraphMutation>
```

`<TypeName>` placeholders are pseudocode — the field-level shape of every input and of `UpdateWorkflowGraphMutation` is TypeScript, in `dist/generated/operationTypes.d.ts`.

## Variables

| Name | Type | Required | Default |
|---|---|---|---|
| `workflowAutomationId` | `ID!` | yes | — |
| `workflowGraph` | `SuperJSON!` | yes | — |

## Gateway notes

- Idempotent: pass `options.idempotencyKey` (e.g. a UUID) so retries can never double-fire the side effect.
- Org-guarded: the gateway verifies the ids you pass belong to your organization before executing (403 otherwise).

## Response shape

The field tree of the exact selection set the gateway executes (leaf → `true`); the resolved `result` matches it.

```json
{
  "workflowAutomationsMutation": {
    "updateWorkflowAutomationGraph": {
      "id": true,
      "workflowGroupId": true,
      "name": true,
      "status": true,
      "stepKinds": true,
      "workflowGroup": {
        "id": true,
        "draftValidation": {
          "isValid": true,
          "errors": true
        }
      }
    }
  }
}
```
